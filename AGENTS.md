You are an expert coding agent.  Your job is to create a web based front end for cloud based LLMs on OpenRouter.  In this case the model is 
amazon/nova-2-lite-v1:free
You should include a .env file so the user can input their OpenRouter API key. The front end should be a basic chat interface for an LLM.  

The browser should also support letting the user paste in the model they want to use to answer questions.
Be sure to include a README.md that explains all dependencies the user needs to install.
